# orbit-worker — companion service fleet (the multi-root demo: its own Bridge/namespace).
agents:
  helper:
    cmd: claude
    role: coder
    instructions: you maintain the telemetry worker

terminals:
  worker:
    cmd: npm start
    autostart: true
  scratch:
    cmd: bash

commands:
  test: {cmd: npm test}
