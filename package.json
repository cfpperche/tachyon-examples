{
  "name": "tachyon-examples",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["orbit-api", "orbit-worker"],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "dev": "npm run dev --workspace orbit-api"
  }
}
